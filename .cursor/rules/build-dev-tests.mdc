---
description: Run .\build-release.ps1 for builds and verification; never ask the user to build
alwaysApply: true
---

# Build and verification

After making code changes in this repo, **run the release build yourself**. Do not ask the
user to run it unless you are blocked (permissions, missing tooling, or environment
you cannot access).

## Command

From `NMSFM_FGF_CVE/NMSFMFireGrantWF/`:

```powershell
.\build-release.ps1
```

Always use `.\build-release.ps1` — not `.\build.ps1`. Release is the default configuration.

## When to run

- After editing C#, ASPX, project, or config files that affect the web app
- Before telling the user a change is ready to test
- After fixing compile, audit, or build errors

## Agent behavior

- Run `.\build-release.ps1` in the terminal; do not delegate to the user
- If the build fails, fix issues and re-run until it succeeds or you are blocked
- Report build result briefly (success or what failed)
- Do not say "please rebuild" or "run build-release.ps1" when you can run it yourself
- If you already built successfully in the same session after the latest changes,
  do not ask for another build; tell the user to restart the app if runtime refresh
  is needed

## Working directory

Always execute from:

`NMSFM_FGF_CVE/NMSFMFireGrantWF/`

Not the repo root. Use `publish/` only when inspecting deploy output.
